<VirtualHost *:9888>
     # Add machine's IP address (use ifconfig command)
     ServerName 10.0.10.114

        LogLevel info

     DocumentRoot /root/pns

     <Directory /root/pns>
     	<IfVersion < 2.4>
          Order allow,deny
     	  Allow from all
    	</IfVersion>
    	<IfVersion >= 2.4>
     	  Require all granted
    	</IfVersion>
    </Directory>

    # WSGIDaemonProcess pns /v36/env36/lib/python3.6/site-packages
    # WSGIProcessGroup pns

      WSGIScriptAlias / /root/svom/fdi/pns/resources/pns.wsgi
     #WSGIScriptAlias /testHello /ying/helloworld/hello.py

      <Directory /root/svom>
     #<Directory /ying/helloworld>
          # AllowOverride None
            Require all granted
          # Order allow,deny
          # Allow from all
     </Directory>
</VirtualHost>